import { CSSProperties } from "react";

const carouselContainer: CSSProperties = {
    margin: '20px',
    display: 'flex',
    alignItems: 'center'
}

const imageContent: CSSProperties = {
    width: 200, 
    height:200
}

const button:CSSProperties= {
    margin: 10
}

export { carouselContainer, imageContent, button};