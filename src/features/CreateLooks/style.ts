import { CSSProperties } from "react";

const createLooksContainer: CSSProperties = { 
    padding: '20px',
    display: 'flex',
    alignItems: 'center', 
    flexDirection: 'column' ,
    backgroundColor: '#f5f5f5',
    minHeight: '100vh',
}

export { createLooksContainer };
